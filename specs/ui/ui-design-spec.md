# Synnovator UI 设计规范

本文档定义 Synnovator Hackathon 平台的完整 UI 设计规范，基于 Neon Forge 主题和 16 个用户旅程。

> **设计系统：** Neon Forge
> **前端框架：** Next.js 14 + Tailwind CSS v4 + shadcn/ui
> **参考平台：** Devpost, HackerEarth, MLH, Devfolio

---

## shadcn/ui 组件映射表

以下表格列出 UI 设计中所有组件与 shadcn/ui 组件的对应关系，用于快速原型实现。

### 布局组件

| UI 组件 | shadcn 组件 | 备注 |
|---------|-------------|------|
| Header | 自定义布局 | 使用 flex 布局 |
| Sidebar | `sidebar` | 使用 sidebar-07 或 sidebar-10 变体 |
| Panel | `sheet` / `card` | 右侧面板用 sheet，内容区用 card |
| Body | 自定义布局 | 使用 flex-grow + overflow-auto |
| Footer | 自定义布局 | 使用 flex 布局 |

### 导航组件

| UI 组件 | shadcn 组件 | 安装命令 |
|---------|-------------|---------|
| Search Bar | `command` | `npx shadcn@latest add command` |
| Navigation Links | `navigation-menu` | `npx shadcn@latest add navigation-menu` |
| Notification Bell | `popover` + `dropdown-menu` | `npx shadcn@latest add popover dropdown-menu` |
| Add New Menu | `dropdown-menu` | `npx shadcn@latest add dropdown-menu` |
| Avatar + Menu | `avatar` + `dropdown-menu` | `npx shadcn@latest add avatar dropdown-menu` |
| Tab 栏 | `tabs` | `npx shadcn@latest add tabs` |
| Breadcrumb | `breadcrumb` | `npx shadcn@latest add breadcrumb` |
| Pagination | `pagination` | `npx shadcn@latest add pagination` |

### 按钮组件

| UI 变体 | shadcn variant | 用法 |
|---------|---------------|------|
| Primary | `default` | `<Button>主要操作</Button>` |
| Secondary | `secondary` | `<Button variant="secondary">次要操作</Button>` |
| Ghost | `ghost` | `<Button variant="ghost">取消</Button>` |
| Danger | `destructive` | `<Button variant="destructive">删除</Button>` |
| Link | `link` | `<Button variant="link">链接</Button>` |

### 卡片组件

| UI 组件 | shadcn 组件 | 结构 |
|---------|-------------|------|
| Event Card | `card` | Card + CardHeader + CardContent + CardFooter |
| Post Card | `card` | Card + CardHeader + CardContent + CardFooter |
| Group Card | `card` | Card + Avatar Group + CardContent |
| User Card | `card` | Card + Avatar + CardContent |

### 表单组件

| UI 组件 | shadcn 组件 | 备注 |
|---------|-------------|------|
| Input | `input` | `npx shadcn@latest add input` |
| Select | `select` | `npx shadcn@latest add select` |
| Tag Input | `input` + `badge` | 自定义：输入框 + badge 列表 |
| File Upload | 原生 `input[type=file]` | 简单实现，后续可升级 |
| Markdown Editor | `textarea` | 简单 textarea，后续可升级 |
| Comment Input | `textarea` | 纯文本评论 |
| Checkbox | `checkbox` | `npx shadcn@latest add checkbox` |
| Radio Group | `radio-group` | `npx shadcn@latest add radio-group` |

### 数据展示组件

| UI 组件 | shadcn 组件 | 用法 |
|---------|-------------|------|
| Tag | `badge` | `<Badge variant="secondary">AI</Badge>` |
| Status Badge | `badge` | `<Badge variant="default">进行中</Badge>` |
| Avatar | `avatar` | `<Avatar><AvatarImage /><AvatarFallback /></Avatar>` |
| Empty State | 自定义 | 使用 flex + icon + text |

### 反馈组件

| UI 组件 | shadcn 组件 | 安装命令 |
|---------|-------------|---------|
| Toast | `sonner` | `npx shadcn@latest add sonner` |
| Dialog | `dialog` | `npx shadcn@latest add dialog` |
| Alert Dialog | `alert-dialog` | `npx shadcn@latest add alert-dialog` |
| Spinner | `spinner` 或自定义 | Tailwind animate-spin |
| Skeleton | `skeleton` | `npx shadcn@latest add skeleton` |

### 快速安装所有组件

```bash
# 安装所有常用组件
npx shadcn@latest add button card avatar badge tabs dialog alert-dialog dropdown-menu popover command input textarea select checkbox radio-group pagination breadcrumb navigation-menu skeleton sonner sidebar sheet separator scroll-area tooltip
```

---

## 目录

- [1. 全局布局架构](#1-全局布局架构)
- [2. Header（顶部导航）](#2-header顶部导航)
- [3. Sidebar（侧边栏）](#3-sidebar侧边栏)
- [4. Body（主内容区）](#4-body主内容区)
- [5. Panel（右侧面板）](#5-panel右侧面板)
- [6. Footer（页脚）](#6-footer页脚)
- [7. 页面设计](#7-页面设计)
- [8. 组件库](#8-组件库)
- [附录：User Journey 与 UI/API 对照表](#附录user-journey-与-uiapi-对照表)

---

## 1. 全局布局架构

### 1.1 布局网格

```
┌─────────────────────────────────────────────────────────────────┐
│                         A. HEADER (60px)                        │
├────────────┬────────────────────────────────┬───────────────────┤
│            │                                │                   │
│     B.     │                                │        E.         │
│  SIDEBAR   │           D. BODY              │      PANEL        │
│  (168px)   │         (flex-grow)            │     (328px)       │
│            │                                │                   │
│            │                                │                   │
├────────────┴────────────────────────────────┴───────────────────┤
│                         C. FOOTER (auto)                        │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 布局变体

| 变体 | Sidebar | Panel | 适用页面 |
|------|---------|-------|---------|
| **Full** | 展开 (168px) | 显示 (328px) | 活动详情、帖子详情 |
| **Compact** | 收起 (60px) | 显示 | 浏览探索页 |
| **Focus** | 隐藏 | 隐藏 | 编辑器、表单页 |
| **Landing** | 隐藏 | 隐藏 | 首页、登录注册 |

### 1.3 响应式断点

| 断点 | 宽度 | Sidebar | Panel |
|------|------|---------|-------|
| Desktop XL | ≥1440px | 展开 | 显示 |
| Desktop | ≥1200px | 收起 | 显示 |
| Tablet | ≥768px | 隐藏 | 抽屉 |
| Mobile | <768px | 隐藏 | 隐藏 |

---

## 2. Header（顶部导航）

高度：60px | 背景：`--nf-surface (#181818)` | 固定定位

> **对应 Journey：** J2 浏览探索页, J3 注册, J4 登录, J9 发送帖子

### 2.1 组件结构（从左至右）

```
┌─────────────────────────────────────────────────────────────────────────┐
│  [Logo]  [Search Bar          🔍]  [Explore] [Events]    🔔  [+] [Avatar]│
│  ▲       ▲                         ▲                     ▲   ▲   ▲      │
│  A       B                         C                     D   E   F      │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.2 组件详情

#### A. Logo
- **类型：** 图片 + 文字组合
- **图片：** 32x32px，圆角 8px
- **文字：** "Synnovator"，OPPOSans Bold 18px，`--nf-white`
- **点击：** 跳转首页 (`/`)
- **间距：** 左边距 24px，Logo 与文字间距 8px

#### B. Search Bar
- **宽度：** 320px（桌面）/ 200px（平板）/ 图标化（移动端）
- **高度：** 40px
- **背景：** `--nf-dark (#222222)`
- **边框：** 1px solid `--nf-secondary (#333333)`
- **圆角：** 20px (Pill)
- **Placeholder：** "搜索活动、帖子、用户..."，`--nf-muted (#8E8E8E)`
- **图标：** Search icon，右侧 12px
- **快捷键提示：** `⌘K` 徽章，右侧
- **API：** `GET /events?search={query}` + `GET /posts?search={query}` + `GET /users?search={query}`
- **⚠️ 组合实现：** 需分别调用三个列表接口进行搜索

#### C. Navigation Links
- **项目：**
  - Explore（探索）→ `/explore` (`GET /posts`)
  - Events（活动）→ `/events` (`GET /events`)
- **样式：** 文字链接，Inter 14px，`--nf-light-gray`
- **Hover：** `--nf-white`，下划线
- **Active：** `--nf-lime (#BBFD3B)`，2px 底部边框
- **间距：** 各项间距 24px

#### D. Notification Bell
- **图标：** Bell icon，24x24px
- **颜色：** `--nf-light-gray`，Hover 时 `--nf-white`
- **徽章：** 未读数量，红色圆点 8px（>0 时显示）
- **点击：** 下拉通知面板
- **API：** `🚨 Not Implemented` - 需要 `GET /notifications` 接口

**通知下拉面板：**
```
┌─────────────────────────────────┐
│ 通知                    全部标记已读 │
├─────────────────────────────────┤
│ 🏆 你的提案获得一等奖     2小时前 │
│ 💬 Alice 回复了你的评论   昨天   │
│ 👥 Bob 申请加入你的团队   2天前  │
├─────────────────────────────────┤
│        查看全部通知 →           │
└─────────────────────────────────┘
```
- **API：** `🚨 Not Implemented` - 需要通知系统接口

#### E. Add New Post Button（对应 J9 发送帖子）
- **类型：** Icon Button
- **图标：** Plus icon，20x20px
- **背景：** `--nf-lime (#BBFD3B)`
- **大小：** 36x36px
- **圆角：** 50%
- **点击：** 下拉菜单

**创建下拉菜单：**
```
┌─────────────────────────┐
│ 📝 发布日常帖 → POST /posts (type: general)
│ 💡 发布提案   → POST /posts (type: proposal)
│ 👥 创建团队   → POST /groups
│ 📅 创建活动   → POST /events (仅组织者)
└─────────────────────────┘
```

#### F. Avatar（对应 J4 登录）
- **类型：** 圆形头像
- **大小：** 36x36px
- **边框：** 2px solid `--nf-secondary`
- **默认：** 首字母 + 渐变背景
- **点击：** 下拉用户菜单
- **API：** `GET /users/me` (avatar_url 字段)

**用户下拉菜单：**
```
┌─────────────────────────┐
│ 👤 我的主页 → /users/{user_id} (GET /users/{user_id})
│ 📊 我的活动 → /my/events (GET /events?created_by=me)
│ 👥 我的团队 → /my/groups (GET /groups?member=me)
│ ⚙️ 设置    → /settings (PATCH /users/me)
├─────────────────────────┤
│ 🚪 退出登录 → POST /auth/logout 🚨 Not Implemented
└─────────────────────────┘
```

### 2.3 未登录状态（对应 J3 注册, J4 登录）

替换 D/E/F 为：
```
[登录] → /login (🚨 Not Implemented: POST /auth/login)
[注册] → /register (🚨 Not Implemented: POST /auth/register)
```
- **登录：** Ghost Button，`--nf-white` 文字
- **注册：** Primary Button，`--nf-lime` 背景

---

## 3. Sidebar（侧边栏）

宽度：168px（展开）/ 60px（收起）| 背景：`--nf-surface` | 固定定位

> **对应 Journey：** J2 浏览探索页, J5 加入组, J7 加入活动, J14 关注与好友

### 3.1 结构

```
┌──────────────────┐
│    展开/收起 ≡   │
├──────────────────┤
│ 🏠 首页          │ → / (Landing Page)
│ 🔍 探索          │ → /explore (GET /posts + GET /events)
│ 📅 活动          │ → /events (GET /events)
│ 📝 帖子          │ → /posts (GET /posts)
│ 👥 团队          │ → /groups (GET /groups)
├──────────────────┤
│ --- 我的 ---     │
│ 📌 我参与的活动  │ → /my/events (GET /events?participant=me) ⚠️ 组合
│ ✍️ 我的帖子     │ → /my/posts (GET /posts?created_by={user_id})
│ 👤 我的团队      │ → /my/groups (GET /groups?member={user_id}) ⚠️ 组合
│ ❤️ 我的收藏     │ → /my/favorites 🚨 Not Implemented
│ 👣 关注          │ → /my/following 🚨 Not Implemented (J14)
├──────────────────┤
│                  │
│                  │
│ ⚙️ 设置         │ → /settings (GET/PATCH /users/me)
└──────────────────┘
```

### 3.2 导航项样式

| 状态 | 背景 | 文字颜色 | 图标颜色 |
|------|------|---------|---------|
| Default | transparent | `--nf-muted` | `--nf-muted` |
| Hover | `--nf-dark` | `--nf-white` | `--nf-white` |
| Active | `--nf-dark` | `--nf-lime` | `--nf-lime` |

- **高度：** 44px
- **圆角：** 8px
- **图标：** 20x20px，左侧
- **文字：** Inter 14px
- **收起时：** 仅显示图标，Tooltip 显示名称

### 3.3 分组标题

- **样式：** 大写字母，OPPOSans 10px，`--nf-muted`
- **间距：** 上方 24px，下方 8px

---

## 4. Body（主内容区）

### 4.1 通用规范

- **背景：** `--nf-dark (#222222)`
- **内边距：** 32px（桌面）/ 24px（平板）/ 16px（移动端）
- **最大宽度：** 1200px（无 Panel 时居中）
- **滚动：** 独立滚动区域

### 4.2 页面标题区

```
┌─────────────────────────────────────────────────┐
│ 🔥 探索                                         │
│ 发现最新的 Hackathon 活动和精彩项目             │
├─────────────────────────────────────────────────┤
│ [全部] [进行中] [即将开始] [已结束]    [筛选 ▾] │
└─────────────────────────────────────────────────┘
```

- **标题：** OPPOSans Bold 24px，`--nf-white`
- **描述：** Inter 14px，`--nf-muted`
- **Tab 栏：** 下方 24px
- **筛选 API：** `GET /events?status={status}` 或 `GET /posts?status={status}&tags={tags}`

### 4.3 内容网格

**活动/帖子卡片网格：**
- **Desktop XL：** 4 列
- **Desktop：** 3 列
- **Tablet：** 2 列
- **Mobile：** 1 列
- **间距：** 24px

---

## 5. Panel（右侧面板）

宽度：328px | 背景：`--nf-surface` | 固定定位

> **对应 Journey：** J7 加入活动, J8 创建团队, J10 活动结束与颁奖, J13 社区互动

### 5.1 结构（活动详情页示例）

```
┌────────────────────────────┐
│ 📊 活动概览            ✕  │
├────────────────────────────┤
│ ┌────────────────────────┐ │
│ │  活动名称 / 帖子标题   │ │  → GET /events/{id}
│ │  创建者 · 时间        │ │
│ └────────────────────────┘ │
├────────────────────────────┤
│ 👥 团队成员 (3/5)          │  → GET /groups/{id}/members
│ ┌──┐ ┌──┐ ┌──┐            │
│ │  │ │  │ │  │ + 邀请     │  → POST /groups/{id}/members
│ └──┘ └──┘ └──┘            │
├────────────────────────────┤
│ 📅 重要日期                │  → GET /events/{id}/rules
│ • 报名截止: 2024-03-15    │     (submission_start, submission_deadline)
│ • 提交截止: 2024-03-20    │
│ • 结果公布: 2024-03-25    │
├────────────────────────────┤
│ 📋 规则摘要                │  → GET /events/{id}/rules
│ • 最多 5 人/团队          │     (min_team_size, max_team_size)
│ • 提交需包含 PDF          │     (submission_format)
│ • 每人限提交 1 次         │     (max_submissions)
├────────────────────────────┤
│ [报名参赛]                 │  → POST /events/{id}/groups (J7)
└────────────────────────────┘
```

### 5.2 Panel 变体

| 上下文 | Panel 内容 | API |
|--------|-----------|-----|
| 活动详情页 | 活动概览、团队、日期、规则、报名按钮 | `GET /events/{id}`, `GET /events/{id}/rules`, `GET /events/{id}/groups` |
| 帖子详情页 | 作者信息、相关帖子、互动统计 | `GET /users/{id}`, `GET /posts/{id}/related`, `GET /posts/{id}` (like_count, comment_count) |
| 用户主页 | 用户统计、关注按钮、最近活动 | `GET /users/{id}`, `🚨 Not Implemented: POST /users/{id}/follow` |
| 团队详情页 | 成员列表、参与活动、团队统计 | `GET /groups/{id}/members`, `GET /events?group={id}` |

### 5.3 样式规范

- **Section 标题：** OPPOSans 14px，`--nf-muted`
- **Section 间距：** 24px
- **卡片背景：** `--nf-dark`
- **卡片圆角：** 12px
- **卡片内边距：** 16px

---

## 6. Footer（页脚）

高度：auto | 背景：`--nf-surface` | 仅在滚动到底部显示

### 6.1 结构

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│  [Logo] Synnovator                                              │
│  创意协作平台                     关于我们  帮助中心  联系我们  │
│                                   隐私政策  服务条款  API 文档  │
│                                                                 │
│  ─────────────────────────────────────────────────────────────  │
│  © 2024 Synnovator. All rights reserved.    [Twitter] [GitHub] │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 6.2 样式

- **Logo 区：** 左对齐
- **链接组：** 右对齐，两列
- **链接样式：** Inter 14px，`--nf-muted`，Hover `--nf-white`
- **分隔线：** 1px solid `--nf-secondary`
- **版权文字：** Inter 12px，`--nf-muted`
- **社交图标：** 20x20px，`--nf-muted`

---

## 7. 页面设计

### 7.1 首页 (Landing Page)

**布局：** Landing 变体（无 Sidebar/Panel）

> **对应 Journey：** J2 浏览探索页

```
┌─────────────────────────────────────────────────────────────────┐
│                           HEADER                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│               🚀 创意在这里起飞                                 │
│        发现最激动人心的 Hackathon，与志同道合的人组队           │
│                                                                 │
│        [探索活动]              [发起活动]                       │
│         → /events               → /events/create (组织者)       │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│  🔥 热门活动                                          查看更多 → │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐              │
│  │ 活动卡片 │ │ 活动卡片 │ │ 活动卡片 │ │ 活动卡片 │              │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘              │
│  API: GET /events?status=published&limit=4                  │
├─────────────────────────────────────────────────────────────────┤
│  💡 精选项目                                          查看更多 → │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐              │
│  │ 帖子卡片 │ │ 帖子卡片 │ │ 帖子卡片 │ │ 帖子卡片 │              │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘              │
│  API: GET /posts?status=published&type=proposal&limit=4     │
├─────────────────────────────────────────────────────────────────┤
│  📊 平台数据                                                    │
│       1000+           500+            200+           50+        │
│       活动            团队            项目           获奖作品    │
│  API: 🚨 Not Implemented (需要统计接口 GET /stats)              │
├─────────────────────────────────────────────────────────────────┤
│                           FOOTER                                │
└─────────────────────────────────────────────────────────────────┘
```

### 7.2 探索页 (Explore)

**布局：** Compact 变体

> **对应 Journey：** J2 浏览探索页

```
┌─────────────────────────────────────────────────────────────────┐
│                           HEADER                                │
├──────┬──────────────────────────────────────────────────────────┤
│      │  🔍 探索                                                 │
│  S   │  ┌─────────────────────────────────────────────────────┐ │
│  I   │  │ [全部] [活动] [帖子] [团队] [用户]    [筛选▾] [排序▾]│ │
│  D   │  └─────────────────────────────────────────────────────┘ │
│  E   │  API: GET /events | GET /posts | GET /groups | GET /users
│  B   │                                                          │
│  A   │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐        │
│  R   │  │         │ │         │ │         │ │         │        │
│      │  │  卡片   │ │  卡片   │ │  卡片   │ │  卡片   │        │
│      │  │         │ │         │ │         │ │         │        │
│      │  └─────────┘ └─────────┘ └─────────┘ └─────────┘        │
│      │                                                          │
│      │  分页: ?skip={offset}&limit={limit}                      │
└──────┴──────────────────────────────────────────────────────────┘
```

**筛选面板：**
```
┌─────────────────────────┐
│ 筛选                 ✕  │
├─────────────────────────┤
│ 状态                    │  → ?status=published|draft|closed
│ ○ 全部                  │
│ ● 进行中               │
│ ○ 即将开始             │
│ ○ 已结束               │
├─────────────────────────┤
│ 类型                    │  → ?type=competition|operation
│ ☑ Hackathon            │
│ ☑ 编程马拉松           │
│ ☐ 设计挑战             │
├─────────────────────────┤
│ 标签                    │  → ?tags=AI,Web3
│ [AI] [Web3] [Climate]   │
├─────────────────────────┤
│ [重置]      [应用筛选]  │
└─────────────────────────┘
```

### 7.3 活动详情页 (Event Detail)

**布局：** Full 变体

> **对应 Journey：** J6 创建活动, J7 加入活动, J10 活动结束与颁奖

```
┌─────────────────────────────────────────────────────────────────┐
│                           HEADER                                │
├──────┬────────────────────────────────────────┬─────────────────┤
│      │  ← 返回                                │                 │
│  S   │  ┌───────────────────────────────────┐ │    📊 活动概览  │
│  I   │  │         活动封面图                │ │                 │
│  D   │  └───────────────────────────────────┘ │    状态: 进行中 │
│  E   │  API: GET /events/{id}             │    报名: 50/100 │
│  B   │                                        │                 │
│  A   │  🏆 AI 创新挑战赛 2024                │    ─────────────│
│  R   │  由 TechCorp 主办 · 2024.03.01-03.30  │                 │
│      │                                        │    👥 我的团队  │
│      │  [📋 详情] [📝 提交] [👥 团队] [🏅 排名]│    GET /groups/{id}/members
│      │                                        │    ┌──┐┌──┐┌──┐ │
│      │  Tab: 详情 → GET /events/{id}      │    │  ││  ││  │ │
│      │  Tab: 提交 → GET /events/{id}/posts│    └──┘└──┘└──┘ │
│      │  Tab: 团队 → GET /events/{id}/groups    + 邀请成员   │
│      │  Tab: 排名 → GET /events/{id}/posts     POST /groups/{id}/members
│      │              ?sort=average_rating      │                 │
│      │  ─────────────────────────────────────│    ─────────────│
│      │                                        │                 │
│      │  ## 活动介绍                           │    📅 重要日期  │
│      │  (Markdown 内容)                       │    GET /events/{id}/rules
│      │                                        │    报名截止     │
│      │  ## 奖项设置                           │    03-15 23:59  │
│      │  - 一等奖: ¥50,000                    │                 │
│      │  - 二等奖: ¥30,000                    │    提交截止     │
│      │  - 三等奖: ¥10,000                    │    03-20 23:59  │
│      │                                        │                 │
│      │  ## 评分标准                           │    ─────────────│
│      │  GET /events/{id}/rules            │                 │
│      │  (scoring_criteria 字段)               │  [报名参赛]     │
│      │                                        │  POST /events/{id}/groups
│      │                                        │                 │
└──────┴────────────────────────────────────────┴─────────────────┘
```

### 7.4 帖子详情页 (Post Detail)

**布局：** Full 变体

> **对应 Journey：** J9 发送帖子, J11 编辑 Post, J12 删除 Post, J13 社区互动

```
┌─────────────────────────────────────────────────────────────────┐
│                           HEADER                                │
├──────┬────────────────────────────────────────┬─────────────────┤
│      │  ← 返回                                │                 │
│  S   │  API: GET /posts/{id}                  │    👤 作者信息  │
│  I   │                                        │    GET /users/{id}
│  D   │  💡 基于 LLM 的智能助手平台           │    ┌──────────┐ │
│  E   │                                        │    │   头像   │ │
│  B   │  ┌──┐ Alice · 2024-03-10              │    │  Alice   │ │
│  A   │  └──┘ Team Innovators                  │    │ @alice   │ │
│  R   │       #AI #LLM #ChatBot               │    └──────────┘ │
│      │                                        │    [+ 关注]     │
│      │  操作按钮 (作者可见):                  │    🚨 Not Implemented
│      │  [编辑] → /posts/{id}/edit             │    POST /users/{id}/follow
│      │          PATCH /posts/{id}             │                 │
│      │  [删除] → DELETE /posts/{id}           │    ─────────────│
│      │  ─────────────────────────────────────│                 │
│      │                                        │    📊 互动统计  │
│      │  (Markdown 内容渲染)                   │    GET /posts/{id}
│      │                                        │    ❤️ 128 点赞  │
│      │  ## 团队介绍                           │    💬 32 评论   │
│      │  ┌─────────────────────────┐          │    ⭐ 4.5 评分  │
│      │  │   团队卡片嵌入          │          │                 │
│      │  │   GET /posts/{id}/related│          │    ─────────────│
│      │  │   ?relation_type=embed  │          │                 │
│      │  └─────────────────────────┘          │    📎 附件      │
│      │                                        │    GET /posts/{id}/resources
│      │  ─────────────────────────────────────│    📄 proposal.pdf
│      │                                        │    📦 demo.zip   │
│      │  💬 评论 (32)                         │                 │
│      │  GET /posts/{id}/comments              │    ─────────────│
│      │  ┌───────────────────────────────────┐│                 │
│      │  │ 输入评论...              [发送]   ││    🔗 相关帖子  │
│      │  │ POST /posts/{id}/comments         ││    GET /posts/{id}/related
│      │  └───────────────────────────────────┘│    • 项目 A     │
│      │                                        │    • 项目 B     │
│      │  ┌───────────────────────────────────┐│                 │
│      │  │ Bob: 很棒的项目！                 ││                 │
│      │  │ [❤️ 点赞] POST /posts/{id}/like   ││                 │
│      │  │ [回复] POST /posts/{id}/comments  ││                 │
│      │  │        (parent_id: comment_id)    ││                 │
│      │  └───────────────────────────────────┘│                 │
└──────┴────────────────────────────────────────┴─────────────────┘
```

### 7.5 创建/编辑帖子页

**布局：** Focus 变体

> **对应 Journey：** J9 发送帖子, J11 编辑 Post

```
┌─────────────────────────────────────────────────────────────────┐
│  [✕ 取消]              创建帖子                    [保存草稿] [发布] │
│                        POST /posts                 status=draft  status=published
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  帖子类型 (type 字段)                                           │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐              │
│  │ 📝 日常 │ │ 💡 提案 │ │ 👥 团队 │ │ 👤 个人 │              │
│  │ general │ │for_categ│ │  team   │ │ profile │              │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘              │
│                                                                 │
│  标题 * (title 字段)                                            │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 输入帖子标题...                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  内容 * (content 字段，Markdown)                                │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ [B] [I] [H1] [H2] [链接] [图片] [代码] [引用]           │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │                                                         │   │
│  │  Markdown 编辑器                                        │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  标签 (tags 字段，数组)                                         │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ [AI ✕] [Web3 ✕] + 添加标签                              │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  附件                                                           │
│  POST /resources → POST /posts/{id}/resources                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  📄 proposal.pdf (2.3 MB)                          [✕]  │   │
│  │  DELETE /posts/{id}/resources/{resource_id}             │   │
│  │  ┌─────────────────────────────────────────────────┐   │   │
│  │  │       拖拽文件到此处或点击上传                   │   │   │
│  │  └─────────────────────────────────────────────────┘   │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  关联活动（可选，type=proposal 时）                         │
│  POST /events/{id}/posts                                    │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 选择要提交的活动...                                 ▾   │   │
│  │ GET /events?status=published                        │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  嵌入团队卡片（可选）                                           │
│  POST /posts/{id}/related (relation_type: embed)                │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ + 选择团队                                              │   │
│  │ GET /posts?type=team&created_by={user_id}               │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 7.6 团队详情页

**布局：** Full 变体

> **对应 Journey：** J5 用户登录与加入组, J8 创建团队

```
┌─────────────────────────────────────────────────────────────────┐
│                           HEADER                                │
├──────┬────────────────────────────────────────┬─────────────────┤
│      │  ← 返回                                │                 │
│  S   │  API: GET /groups/{id}                 │    👥 团队操作  │
│  I   │                                        │                 │
│  D   │  ┌──────┐ Team Innovators             │  [申请加入]     │
│  E   │  │ Logo │ 3 成员 · 2 活动参与         │  POST /groups/{id}/members
│  B   │  └──────┘                              │  (status: pending)
│  A   │                                        │                 │
│  R   │  [📋 简介] [👥 成员] [📅 活动] [📝 帖子]│  [分享团队]     │
│      │                                        │                 │
│      │  Tab: 成员 → GET /groups/{id}/members  │    ─────────────│
│      │  Tab: 活动 → GET /events?group={id}│                 │
│      │  Tab: 帖子 → GET /posts?group={id}     │    📊 团队统计  │
│      │  ─────────────────────────────────────│    参与活动: 2  │
│      │                                        │    获奖次数: 1  │
│      │  ## 团队简介                           │    发布帖子: 5  │
│      │  (description 字段)                    │                 │
│      │                                        │    ─────────────│
│      │  ## 成员列表                           │                 │
│      │  GET /groups/{id}/members              │    🏆 荣誉墙   │
│      │  ┌──────────────────────────────────┐ │    🚨 Not Implemented
│      │  │ 👤 Alice (Owner)        [移除]  │ │    (需要奖项接口)
│      │  │    PATCH status=removed          │ │                 │
│      │  │ 👤 Bob (Member)         [移除]  │ │                 │
│      │  │ 👤 Carol (Pending)  [通过][拒绝]│ │                 │
│      │  │    PATCH status=accepted/rejected│ │                 │
│      │  └──────────────────────────────────┘ │                 │
│      │                                        │                 │
│      │  [+ 邀请成员]                          │                 │
│      │  POST /groups/{id}/members             │                 │
│      │                                        │                 │
└──────┴────────────────────────────────────────┴─────────────────┘
```

### 7.7 用户主页

**布局：** Full 变体

> **对应 Journey：** J14 关注与好友

```
┌─────────────────────────────────────────────────────────────────┐
│                           HEADER                                │
├──────┬────────────────────────────────────────┬─────────────────┤
│      │  API: GET /users/{id}                  │                 │
│  S   │  ┌─────────────────────────────────┐   │    👤 用户操作  │
│  I   │  │       封面图                    │   │                 │
│  D   │  └─────────────────────────────────┘   │  [+ 关注]       │
│  E   │  ┌────┐                               │  🚨 Not Implemented
│  B   │  │头像│ Alice                         │  POST /users/{id}/follow
│  A   │  └────┘ @alice · 全栈开发者           │                 │
│  R   │         📍 北京 · 🔗 github.com/alice │  [发消息]       │
│      │                                        │  🚨 Not Implemented
│      │  100 关注 · 50 粉丝 · 10 好友         │                 │
│      │  🚨 Not Implemented                   │    ─────────────│
│      │  GET /users/{id}/following (count)    │                 │
│      │  GET /users/{id}/followers (count)    │    📊 用户统计  │
│      │                                        │    参与活动: 5  │
│      │  [📝 帖子] [👥 团队] [📅 活动] [❤️ 收藏]│    发布帖子: 20 │
│      │                                        │    获赞总数: 500│
│      │  Tab: 帖子 → GET /posts?created_by={id}│                 │
│      │  Tab: 团队 → GET /groups?member={id}   │    ─────────────│
│      │  Tab: 活动 → GET /events?participant={id} ⚠️ 组合    │
│      │  Tab: 收藏 → 🚨 Not Implemented       │    🏷️ 标签     │
│      │  ─────────────────────────────────────│    (from posts)  │
│      │                                        │    [AI] [Web3]  │
│      │  ## 个人简介 (bio 字段)                │    [TypeScript] │
│      │                                        │                 │
│      │  ## 最近帖子                           │                 │
│      │  GET /posts?created_by={id}&limit=3    │                 │
│      │  ┌─────────┐ ┌─────────┐ ┌─────────┐ │                 │
│      │  │ 帖子卡片 │ │ 帖子卡片 │ │ 帖子卡片 │ │                 │
│      │  └─────────┘ └─────────┘ └─────────┘ │                 │
│      │                                        │                 │
└──────┴────────────────────────────────────────┴─────────────────┘
```

### 7.8 活动结束与排名页

**布局：** Full 变体

> **对应 Journey：** J10 活动结束与颁奖

```
┌─────────────────────────────────────────────────────────────────┐
│                           HEADER                                │
├──────┬────────────────────────────────────────┬─────────────────┤
│      │  ← 返回活动                            │                 │
│  S   │  API: GET /events/{id}             │    🏆 颁奖统计  │
│  I   │                                        │                 │
│  D   │  🏆 AI 创新挑战赛 2024 - 获奖名单     │    一等奖: 1    │
│  E   │                                        │    二等奖: 2    │
│  B   │  活动已结束 · 共 50 个提交            │    三等奖: 3    │
│  A   │                                        │    优秀奖: 10   │
│  R   │  ─────────────────────────────────────│                 │
│      │                                        │    ─────────────│
│      │  排名列表                              │                 │
│      │  GET /events/{id}/posts            │    📥 下载      │
│      │      ?relation_type=submission         │    🚨 Not Implemented
│      │      &sort=-average_rating             │    (需要导出接口)
│      │                                        │                 │
│      │  🥇 一等奖                             │                 │
│      │  ┌───────────────────────────────────┐│                 │
│      │  │ Team Innovators                   ││                 │
│      │  │ 智能助手平台                       ││                 │
│      │  │ ⭐ 90.2 分 (average_rating)       ││                 │
│      │  │               [查看项目] [查看证书]││                 │
│      │  │               GET /posts/{id}     ││                 │
│      │  │               GET /posts?type=certificate ⚠️        ││
│      │  └───────────────────────────────────┘│                 │
│      │                                        │                 │
│      │  🥈 二等奖                             │                 │
│      │  ┌───────────────────────────────────┐│                 │
│      │  │ Team Alpha · 85.5 分              ││                 │
│      │  └───────────────────────────────────┘│                 │
│      │                                        │                 │
│      │  组织者操作 (status=published 时可见): │                 │
│      │  [关闭活动] → PATCH /events/{id}  │                 │
│      │               { status: "closed" }    │                 │
│      │                                        │                 │
└──────┴────────────────────────────────────────┴─────────────────┘
```

### 7.9 登录/注册页

**布局：** Landing 变体

> **对应 Journey：** J3 注册, J4 登录

```
┌─────────────────────────────────────────────────────────────────┐
│                           HEADER (简化)                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│                    ┌─────────────────────────┐                  │
│                    │                         │                  │
│                    │    [Logo] Synnovator    │                  │
│                    │                         │                  │
│                    │    [登录] [注册]        │                  │
│                    │    ─────────────────    │                  │
│                    │                         │                  │
│                    │    邮箱                 │                  │
│                    │    ┌─────────────────┐  │                  │
│                    │    │                 │  │                  │
│                    │    └─────────────────┘  │                  │
│                    │                         │                  │
│                    │    密码                 │                  │
│                    │    ┌─────────────────┐  │                  │
│                    │    │                 │  │                  │
│                    │    └─────────────────┘  │                  │
│                    │                         │                  │
│                    │    [登录]               │                  │
│                    │    🚨 Not Implemented   │                  │
│                    │    POST /auth/login     │                  │
│                    │                         │                  │
│                    │    ─── 或 ───           │                  │
│                    │                         │                  │
│                    │    [G] 使用 Google 登录 │                  │
│                    │    🚨 Not Implemented   │                  │
│                    │    GET /oauth/authorize │                  │
│                    │                         │                  │
│                    │    [GitHub] 使用 GitHub │                  │
│                    │    🚨 Not Implemented   │                  │
│                    │                         │                  │
│                    │    还没有账号？注册     │                  │
│                    │    POST /users (注册)   │                  │
│                    │                         │                  │
│                    └─────────────────────────┘                  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 7.10 多阶段/多赛道活动页

**布局：** Full 变体

> **对应 Journey：** J15 多阶段/多赛道活动

```
┌─────────────────────────────────────────────────────────────────┐
│                           HEADER                                │
├──────┬────────────────────────────────────────┬─────────────────┤
│      │  ← 返回                                │                 │
│  S   │                                        │    📊 活动结构  │
│  I   │  🏆 年度创新大赛 2024                 │                 │
│  D   │                                        │    阶段进度:    │
│  E   │  ─────────────────────────────────────│    [1] → [2] → [3]
│  B   │                                        │                 │
│  A   │  ## 赛段/赛道结构                      │    当前: 阶段 1 │
│  R   │  🚨 Not Implemented                   │                 │
│      │  GET /events/{id}/events      │    ─────────────│
│      │                                        │                 │
│      │  ┌─────────┐  ┌─────────┐  ┌─────────┐│    我的进度     │
│      │  │ Stage 1 │→ │ Stage 2 │→ │ Stage 3 ││    阶段 1: ✅   │
│      │  │ 初赛    │  │ 复赛    │  │ 决赛    ││    阶段 2: 🔒   │
│      │  │ ✅完成  │  │ 🔓开放  │  │ 🔒未开放││    阶段 3: 🔒   │
│      │  └─────────┘  └─────────┘  └─────────┘│                 │
│      │                                        │                 │
│      │  ## 或：并行赛道                       │                 │
│      │  ┌─────────┐  ┌─────────┐             │                 │
│      │  │ Track A │  │ Track B │             │                 │
│      │  │ AI 方向 │  │ Web 方向│             │                 │
│      │  └─────────┘  └─────────┘             │                 │
│      │                                        │                 │
│      │  [进入当前阶段]                        │                 │
│      │  → /events/{stage_id}                  │                 │
│      │                                        │                 │
└──────┴────────────────────────────────────────┴─────────────────┘
```

### 7.11 创建活动页

**布局：** Focus 变体

> **对应 Journey：** J6 创建活动

```
┌─────────────────────────────────────────────────────────────────┐
│  [✕ 取消]              创建活动                    [保存草稿] [发布] │
│                        POST /events            status=draft  status=published
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  活动封面                                                        │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │       📷 点击上传封面图 (16:9)                           │   │
│  │       POST /resources → 获取 cover_image URL            │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  活动名称 * (name 字段)                                          │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 输入活动名称，如「AI 创新挑战赛 2024」                    │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  活动类型 (type 字段)                                            │
│  ┌─────────┐ ┌─────────┐                                       │
│  │🏆 竞赛  │ │📋 运营  │                                       │
│  │competition│ │operation│                                       │
│  └─────────┘ └─────────┘                                       │
│                                                                 │
│  活动简介 * (description 字段)                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 简要描述活动内容（将显示在活动卡片上）                     │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  活动详情 * (content 字段，Markdown)                             │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ [B] [I] [H1] [H2] [链接] [图片] [代码] [引用]           │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │                                                         │   │
│  │  Markdown 编辑器                                        │   │
│  │  - 活动介绍                                             │   │
│  │  - 奖项设置                                             │   │
│  │  - 参赛要求                                             │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  标签 (tags 字段)                                                │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ [AI ✕] [Web3 ✕] + 添加标签                              │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ─────────────────────────────────────────────────────────     │
│  📅 时间设置                                                    │
│                                                                 │
│  开始时间 (start_date)          结束时间 (end_date)             │
│  ┌────────────────────┐        ┌────────────────────┐          │
│  │ 2024-03-01         │        │ 2024-03-30         │          │
│  └────────────────────┘        └────────────────────┘          │
│                                                                 │
│  ─────────────────────────────────────────────────────────     │
│  📋 规则设置（可选）                                             │
│                                                                 │
│  [+ 创建活动规则]  → POST /rules + POST /events/{id}/rules  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 已关联规则:                                              │   │
│  │ • 团队人数: 2-5 人 (min_team_size, max_team_size)       │   │
│  │ • 每人限提交 1 次 (max_submissions)                     │   │
│  │ • 提交格式: PDF, ZIP (submission_format)                │   │
│  │ • 需要审核发布 (allow_public: false)                    │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**规则配置弹窗：**
```
┌─────────────────────────────────────┐
│ 配置活动规则                     ✕ │
├─────────────────────────────────────┤
│                                     │
│ 团队人数限制                        │
│ 最少 [2] 人    最多 [5] 人          │
│                                     │
│ 提交限制                            │
│ 每人/每队最多提交 [1] 次            │
│                                     │
│ 提交格式                            │
│ ☑ PDF  ☑ ZIP  ☐ 图片  ☐ 视频       │
│                                     │
│ 发布审核                            │
│ ○ 直接发布                         │
│ ● 需要审核                         │
│                                     │
│ 评分标准 (scoring_criteria)         │
│ ┌─────────────────────────────────┐ │
│ │ 创新性 (30%)                    │ │
│ │ 技术实现 (30%)                  │ │
│ │ 实用价值 (25%)                  │ │
│ │ 演示效果 (15%)                  │ │
│ │ [+ 添加评分维度]                │ │
│ └─────────────────────────────────┘ │
│                                     │
├─────────────────────────────────────┤
│                    [取消] [保存规则] │
└─────────────────────────────────────┘
```

### 7.12 创建团队页

**布局：** Focus 变体

> **对应 Journey：** J8 创建团队

```
┌─────────────────────────────────────────────────────────────────┐
│  [✕ 取消]              创建团队                         [创建] │
│                        POST /groups                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  团队 Logo                                                      │
│  ┌────────────────┐                                            │
│  │                │                                            │
│  │   📷 上传      │  点击上传团队 Logo (1:1)                   │
│  │                │  POST /resources → 获取 logo_url           │
│  └────────────────┘                                            │
│                                                                 │
│  团队名称 * (name 字段)                                         │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 输入团队名称，如「创新先锋队」                            │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  团队简介 (description 字段)                                    │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                                                         │   │
│  │  介绍你的团队，展示团队特色和技术栈...                   │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  可见性 (visibility 字段)                                       │
│  ┌─────────┐ ┌─────────┐                                       │
│  │🌐 公开  │ │🔒 私密  │                                       │
│  │ public  │ │ private │                                       │
│  └─────────┘ └─────────┘                                       │
│  公开团队可被搜索，私密团队仅通过邀请加入                        │
│                                                                 │
│  加入方式 (require_approval 字段)                               │
│  ○ 自由加入 (require_approval: false)                          │
│  ● 需要审批 (require_approval: true)                           │
│                                                                 │
│  ─────────────────────────────────────────────────────────     │
│  👥 邀请成员（可选）                                             │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 🔍 搜索用户名或邮箱...                                   │   │
│  │ GET /users?search={query}                               │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  已邀请:                                                        │
│  ┌──────────────────────────────────┐                          │
│  │ ┌──┐ Alice (@alice)        [✕]  │                          │
│  │ └──┘                             │                          │
│  │ ┌──┐ Bob (@bob)            [✕]  │                          │
│  │ └──┘                             │                          │
│  └──────────────────────────────────┘                          │
│  创建后将自动发送邀请: POST /groups/{id}/members               │
│                                                                 │
│  ─────────────────────────────────────────────────────────     │
│  🎯 关联活动（可选）                                             │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 选择要报名的活动...                                   ▾ │   │
│  │ GET /events?status=published                        │   │
│  └─────────────────────────────────────────────────────────┘   │
│  选择后将自动报名: POST /events/{id}/groups                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**创建成功提示：**
```
┌─────────────────────────────────────┐
│ ✅ 团队创建成功！                   │
├─────────────────────────────────────┤
│                                     │
│ 「创新先锋队」已创建                │
│                                     │
│ 下一步：                            │
│ • 邀请更多成员加入                  │
│ • 报名参加活动                      │
│ • 创建团队介绍帖                    │
│                                     │
├─────────────────────────────────────┤
│ [查看团队]        [继续创建帖子]    │
│ → /groups/{id}    → /posts/create   │
└─────────────────────────────────────┘
```

---

## 8. 组件库

### 8.1 按钮 (Button)

| 变体 | 背景 | 文字 | 边框 | 用途 |
|------|------|------|------|------|
| **Primary** | `--nf-lime` | `--nf-near-black` | none | 主要操作 (POST/创建) |
| **Secondary** | `--nf-secondary` | `--nf-white` | none | 次要操作 |
| **Ghost** | transparent | `--nf-white` | 1px `--nf-secondary` | 取消/返回 |
| **Danger** | `--nf-error` | `--nf-white` | none | 危险操作 (DELETE) |
| **Link** | transparent | `--nf-cyan` | none | 文字链接 |

**尺寸：**
| 尺寸 | 高度 | 内边距 | 字号 |
|------|------|--------|------|
| SM | 32px | 12px 16px | 12px |
| MD | 40px | 12px 24px | 14px |
| LG | 48px | 16px 32px | 16px |

**状态：**
- Hover: 亮度 +10%
- Active: 亮度 -5%
- Disabled: opacity 50%
- Loading: 显示 Spinner

### 8.2 卡片 (Card)

#### 8.2.1 活动卡片 (Event Card)

> **对应 Journey：** J2, J7

```
┌─────────────────────────────────┐
│  ┌─────────────────────────────┐│
│  │       封面图 (16:9)         ││  ← cover_image
│  │                             ││
│  └─────────────────────────────┘│
│  ┌────┐                         │
│  │状态│ 进行中                  │  ← status (published→进行中)
│  └────┘                         │
│  🏆 AI 创新挑战赛 2024          │  ← name
│  TechCorp · 2024.03.01-03.30   │  ← created_by · start_date-end_date
│                                 │
│  [AI] [¥50,000]    👥 50 报名   │  ← tags · 🚨 报名数需组合查询
└─────────────────────────────────┘
```

**API：** `GET /events` 返回列表

**规格：**
- 宽度：自适应网格
- 背景：`--nf-secondary`
- 圆角：12px
- 内边距：16px
- 封面比例：16:9
- 状态徽章：左上角，8px 圆角

**状态徽章颜色：**
| 状态 | 背景 | 文字 |
|------|------|------|
| published (进行中) | `--nf-lime` | `--nf-near-black` |
| draft (草稿) | `--nf-orange` | `--nf-near-black` |
| closed (已结束) | `--nf-muted` | `--nf-white` |

#### 8.2.2 帖子卡片 (Post Card)

> **对应 Journey：** J2, J9

```
┌─────────────────────────────────┐
│  💡 基于 LLM 的智能助手平台     │  ← title
│                                 │
│  我们开发了一个基于大语言模型...│  ← content (截取前100字)
│                                 │
│  ┌──┐ Alice · Team Innovators   │  ← created_by · group (需组合)
│  └──┘ 2024-03-10               │  ← created_at
│                                 │
│  [AI] [LLM]    ❤️ 128 💬 32    │  ← tags · like_count · comment_count
└─────────────────────────────────┘
```

**API：** `GET /posts` 返回列表

**规格：**
- 宽度：自适应网格
- 背景：`--nf-secondary`
- 圆角：12px
- 内边距：16px
- 描述：最多 2 行，超出省略

#### 8.2.3 团队卡片 (Group Card)

> **对应 Journey：** J5, J8

```
┌─────────────────────────────────┐
│  ┌────┐                         │
│  │Logo│ Team Innovators         │  ← name
│  └────┘ 3 成员 · 2 活动参与     │  ← member_count (需 GET /groups/{id}/members)
│                                 │
│  热爱技术，热爱开源...          │  ← description
│                                 │
│  ┌──┐ ┌──┐ ┌──┐                │  ← 成员头像 (GET /groups/{id}/members?limit=3)
│  │  │ │  │ │  │ +2             │
│  └──┘ └──┘ └──┘                │
└─────────────────────────────────┘
```

**API：** `GET /groups` + `GET /groups/{id}/members`

#### 8.2.4 用户卡片 (User Card)

> **对应 Journey：** J14

```
┌─────────────────────────────────┐
│  ┌────┐                         │
│  │头像│ Alice                   │  ← display_name, avatar_url
│  └────┘ @alice · 全栈开发者     │  ← username · bio
│                                 │
│  10 帖子 · 5 活动 · 500 获赞    │  ← 需组合多接口查询
│                                 │
│  [+ 关注]                       │  ← 🚨 Not Implemented
└─────────────────────────────────┘
```

**API：** `GET /users/{id}` + 组合查询统计

### 8.3 表单组件

#### 8.3.1 输入框 (Input)

```
标签 *
┌─────────────────────────────────┐
│ Placeholder...                  │
└─────────────────────────────────┘
帮助文字或错误提示
```

**规格：**
- 高度：40px (MD) / 32px (SM) / 48px (LG)
- 背景：`--nf-dark`
- 边框：1px solid `--nf-secondary`
- 圆角：8px
- 内边距：12px 16px

**状态：**
| 状态 | 边框颜色 |
|------|---------|
| Default | `--nf-secondary` |
| Focus | `--nf-lime` |
| Error | `--nf-error` |
| Disabled | `--nf-secondary` (opacity 50%) |

#### 8.3.2 下拉选择 (Select)

```
标签
┌─────────────────────────────────┐
│ 请选择...                    ▾  │
└─────────────────────────────────┘
```

#### 8.3.3 多选标签 (Tag Input)

```
标签
┌─────────────────────────────────┐
│ [AI ✕] [Web3 ✕] 输入标签...    │
└─────────────────────────────────┘
```

#### 8.3.4 文件上传 (File Upload)

> **对应 Journey：** J9

```
┌─────────────────────────────────────┐
│                                     │
│      📁 拖拽文件到此处或点击上传     │
│      支持 PDF, ZIP, 最大 10MB       │
│                                     │
└─────────────────────────────────────┘
```

**API：** `POST /resources` 上传文件

### 8.4 数据展示

#### 8.4.1 标签 (Tag)

| 变体 | 背景 | 文字 |
|------|------|------|
| Default | `--nf-dark` | `--nf-white` |
| Lime | `--nf-lime` 20% | `--nf-lime` |
| Cyan | `--nf-cyan` 20% | `--nf-cyan` |
| Pink | `--nf-pink` 20% | `--nf-pink` |
| Orange | `--nf-orange` 20% | `--nf-orange` |

**规格：**
- 高度：24px
- 圆角：4px
- 内边距：4px 8px
- 字号：12px

#### 8.4.2 徽章 (Badge)

```
┌──────────┐
│ 进行中   │
└──────────┘
```

**规格：**
- 高度：20px
- 圆角：4px
- 内边距：2px 8px
- 字号：10px

#### 8.4.3 头像 (Avatar)

| 尺寸 | 大小 | 用途 |
|------|------|------|
| XS | 24px | 列表项 |
| SM | 32px | 评论 |
| MD | 40px | 卡片 |
| LG | 64px | 详情页 |
| XL | 96px | 用户主页 |

**规格：**
- 形状：圆形
- 边框：2px solid `--nf-secondary`
- 默认：首字母 + 渐变背景
- **API：** User.avatar_url 字段

#### 8.4.4 空状态 (Empty State)

```
┌─────────────────────────────────┐
│                                 │
│           🔍                    │
│                                 │
│      暂无搜索结果               │
│      试试其他关键词             │
│                                 │
│      [清除筛选]                 │
│                                 │
└─────────────────────────────────┘
```

### 8.5 反馈组件

#### 8.5.1 Toast 提示

```
┌─────────────────────────────────┐
│ ✓ 操作成功                  ✕  │
└─────────────────────────────────┘
```

**位置：** 右上角
**持续时间：** 3 秒
**类型：** Success (绿) / Error (红) / Warning (橙) / Info (蓝)

#### 8.5.2 对话框 (Dialog)

> **对应 Journey：** J12 删除 Post

```
┌─────────────────────────────────────┐
│ 确认删除                         ✕ │
├─────────────────────────────────────┤
│                                     │
│ 确定要删除这个帖子吗？              │
│ 删除后无法恢复。                    │
│                                     │
├─────────────────────────────────────┤
│                    [取消] [确认删除] │
│                            DELETE /posts/{id}
└─────────────────────────────────────┘
```

**规格：**
- 宽度：400px (SM) / 500px (MD) / 600px (LG)
- 背景：`--nf-secondary`
- 圆角：16px
- 遮罩：`--nf-near-black` 70%

#### 8.5.3 加载状态 (Loading)

**Spinner：**
- 大小：24px (SM) / 32px (MD) / 48px (LG)
- 颜色：`--nf-lime`
- 动画：旋转 1s

**Skeleton：**
- 背景：`--nf-dark`
- 动画：shimmer 1.5s

### 8.6 导航组件

#### 8.6.1 Tab 栏

```
[全部] [进行中] [即将开始] [已结束]
  ▲
  └── Active: --nf-lime 底部边框 2px
```

#### 8.6.2 面包屑 (Breadcrumb)

```
首页 / 活动 / AI 创新挑战赛 2024
```

#### 8.6.3 分页 (Pagination)

```
[<] [1] [2] [3] ... [10] [>]
```

**API：** `?skip={offset}&limit={limit}`

---

## 附录：User Journey 与 UI/API 对照表

### 完整对照表

| Journey | UI 入口 | 主要 API | 状态 |
|---------|--------|----------|------|
| **J2 浏览探索页** | 首页、探索页、Header 导航 | `GET /events`, `GET /posts` | ✅ 已实现 |
| **J3 注册** | Header [注册] 按钮、登录页 | `POST /users` | ⚠️ 部分 (缺 auth flow) |
| **J4 登录** | Header [登录] 按钮、登录页 | `🚨 POST /auth/login` | ❌ 未实现 |
| **J5 加入组** | 团队详情页 [申请加入] | `POST /groups/{id}/members` | ✅ 已实现 |
| **J6 创建活动** | Header [+] → 创建活动 | `POST /events`, `POST /rules`, `POST /events/{id}/rules` | ✅ 已实现 |
| **J7 加入活动** | 活动详情页 Panel [报名参赛] | `POST /events/{id}/groups`, `POST /events/{id}/posts` | ✅ 已实现 |
| **J8 创建团队** | Header [+] → 创建团队 | `POST /groups` | ✅ 已实现 |
| **J9 发送帖子** | Header [+] → 发布帖子、创建帖子页 | `POST /posts`, `POST /resources`, `POST /posts/{id}/resources` | ✅ 已实现 |
| **J10 活动结束与颁奖** | 活动详情页 [关闭活动]、排名页 | `PATCH /events/{id}`, `GET /events/{id}/posts?sort=-average_rating` | ⚠️ 部分 (缺自动颁奖) |
| **J11 编辑 Post** | 帖子详情页 [编辑] | `PATCH /posts/{id}` | ✅ 已实现 |
| **J12 删除 Post** | 帖子详情页 [删除] | `DELETE /posts/{id}` | ✅ 已实现 |
| **J13 社区互动** | 帖子详情页点赞/评论/评分 | `POST /posts/{id}/like`, `POST /posts/{id}/comments`, `POST /posts/{id}/ratings` | ✅ 已实现 |
| **J14 关注与好友** | 用户主页 [+ 关注] | `🚨 POST /users/{id}/follow` | ❌ 未实现 |
| **J15 多阶段/多赛道** | 多阶段活动页 | `🚨 GET /events/{id}/events` | ❌ 未实现 |
| **J16 资产转移** | 帖子编辑页附件管理 | `DELETE + POST /posts/{id}/resources` | ✅ 可组合实现 |

### 未实现 API 汇总

| API | 对应 Journey | 说明 |
|-----|-------------|------|
| `POST /auth/login` | J4 | 登录认证 |
| `POST /auth/logout` | J4 | 退出登录 |
| `POST /auth/register` | J3 | 注册（可用 POST /users 替代） |
| `GET /oauth/authorize` | J4 | OAuth 授权 |
| `GET /notifications` | 全局 | 通知列表 |
| `PATCH /notifications/{id}` | 全局 | 标记已读 |
| `POST /users/{id}/follow` | J14 | 关注用户 |
| `DELETE /users/{id}/follow` | J14 | 取消关注 |
| `GET /users/{id}/followers` | J14 | 粉丝列表 |
| `GET /users/{id}/following` | J14 | 关注列表 |
| `POST /users/{id}/block` | J14 | 拉黑用户 |
| `GET /events/{id}/events` | J15 | 活动关联（赛段/赛道） |
| `POST /events/{id}/events` | J15 | 创建活动关联 |
| `GET /stats` | 首页 | 平台统计 |
| `GET /search` | Header | 全局搜索 |

### 可组合实现的查询

| 需求 | 组合方式 |
|------|---------|
| 我参与的活动 | `GET /groups?member=me` → 获取团队 → `GET /events?group={group_id}` |
| 我的团队 | `GET /groups?member={user_id}` (需后端支持筛选) |
| 活动报名数 | `GET /events/{id}/groups` → count |
| 用户帖子数 | `GET /posts?created_by={user_id}` → total |
| 搜索 | 分别调用 `GET /events?search=`, `GET /posts?search=`, `GET /users?search=` |
