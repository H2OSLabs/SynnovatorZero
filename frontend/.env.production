# Production environment configuration
# Loaded automatically by `next build` and `next start`
# Can be overridden by Docker environment variables at runtime

# Backend API URL (relative path, proxied by nginx)
API_URL=/api
INTERNAL_API_URL=http://localhost:8000/api
