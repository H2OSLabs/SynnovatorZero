# 阶段 4: UI 设计文档生成

> 在编写前端代码之前，先生成完整的 UI 设计文档。

## 双分支工作流

根据项目是否有 Figma 设计，选择对应的工作流分支：

```
┌─────────────────────────────────────────────────────────────┐
│               阶段 4: UI/UX 设计检测                          │
│                                                             │
│  检查: specs/design/figma/ 目录存在？                         │
│        或项目有 Figma 设计链接？                               │
└───────────────────────────┬─────────────────────────────────┘
                            │
         ┌──────────────────┴──────────────────┐
         │                                     │
   ┌─────▼─────┐                        ┌─────▼─────┐
   │  是       │                        │  否       │
   └─────┬─────┘                        └─────┬─────┘
         │                                     │
   ┌─────▼───────────────┐              ┌─────▼───────────────┐
   │ 分支 A: 有 Figma      │              │ 分支 B: 无 Figma      │
   │                      │              │                      │
   │ Skills:              │              │ Skill:               │
   │ • figma-resource-    │              │ • ai-ui-generator    │
   │   extractor          │              │                      │
   │ • ui-spec-generator  │              │ 从 User Journey 和   │
   │ • ux-spec-generator  │              │ 测试用例生成 UI 设计  │
   └─────────┬────────────┘              └──────────┬───────────┘
             │                                      │
             └───────────────┬──────────────────────┘
                             │
                   ┌─────────▼─────────┐
                   │ 统一输出:          │
                   │ • specs/design/   │
                   │   pages.yaml      │
                   │ • specs/ux/       │
                   └───────────────────┘
```

---

## 分支 A: 有 Figma 设计

### 适用场景

- 项目有 Figma 设计文件或链接
- `specs/design/figma/` 目录已存在

### Skill 工作流

| 步骤 | Skill | 输入 | 输出 |
|------|-------|------|------|
| 4.1 | `figma-resource-extractor` | Figma URL / 文件 | `specs/design/figma/*.md` |
| 4.2 | `ui-spec-generator` | Figma 导出 + testcases | `specs/design/pages.yaml` |
| 4.3 | `ux-spec-generator` | pages.yaml + testcases | `specs/ux/` |

### 现有设计资源

项目已有完整的 Figma 设计导出文档，位于 `specs/design/figma/`：

| 文件/目录 | 描述 | 数量 |
|----------|------|------|
| `README.md` | 设计索引与使用说明 | - |
| `components.md` | 组件库 | 54 个 |
| `icons.md` | 图标库 | 69 个 |
| `layouts.md` | 布局模板 | - |
| `pages/*.md` | 页面设计 | 14 个 |

**页面设计索引** (`specs/design/figma/pages/`)：

| 页面 | 文件 | 前端路由 |
|------|------|---------|
| 首页 | `home.md` | `/` |
| 探索 | `explore.md` | `/explore` |
| 内容/帖子 | `content.md` | `/posts/*` |
| 团队 | `team.md` | `/groups/*` |
| 用户资料 | `profile.md` | `/users/*` |
| 设置 | `settings.md` | `/settings` |
| 活动/星球 | `planet.md` | `/events/*` |
| 认证 | `auth.md` | `/login`, `/register` |
| 搜索 | `search.md` | 搜索模态框 |
| 消息 | `message.md` | 消息模态框 |
| 营地 | `camp.md` | `/camp/*` |
| 资产 | `asset.md` | 资产管理 |
| 其他 | `misc.md` | 杂项页面 |

> **使用原则**: 设计文档作为参考，优先实现核心功能，不追求像素级还原。

### 执行示例

```bash
# 步骤 1: 提取 Figma 资源（如果需要更新）
# 调用 figma-resource-extractor skill

# 步骤 2: 生成 UI 规范
# 调用 ui-spec-generator skill

# 步骤 3: 生成 UX 规范
# 调用 ux-spec-generator skill
```

---

## 分支 B: 无 Figma 设计

### 适用场景

- 新原型项目，没有 Figma 设计
- 需要快速生成 UI/UX 设计文档
- 希望从 User Journey 自动生成界面

### Skill: ai-ui-generator

使用 Claude + shadcn/ui 组合，从 User Journey 和测试用例生成结构化的 UI/UX 规范。

#### 输入

| 文件 | 描述 |
|------|------|
| `docs/user-journeys/*.md` | 用户旅程文档 |
| `specs/testcases/*.md` | 测试用例 |
| Design System (Neon Forge) | 主题和样式规范 |

#### 输出

| 文件 | 描述 |
|------|------|
| `specs/design/pages.yaml` | 页面-组件层级结构 |
| `specs/design/component-install.sh` | shadcn 组件安装脚本 |
| `specs/ux/README.md` | UX 规范概述 |
| `specs/ux/pages/{page}.yaml` | 每页交互规范 |
| `specs/ux/flows/{flow}.yaml` | 用户流程规范 |

#### 工作流程

1. **读取 User Journeys**
   - 解析所有用户旅程文档
   - 提取页面需求、组件需求、用户操作

2. **读取测试用例**
   - 提取表单字段、验证规则
   - 识别成功/错误状态

3. **组件映射**
   - 使用 shadcn MCP 查找合适组件
   - 遵循 Neon Forge 主题规范

4. **生成规范文件**
   - 输出 pages.yaml（与分支 A 兼容）
   - 输出 UX 交互规范

#### 执行示例

```bash
# 调用 ai-ui-generator skill
# 输入: docs/user-journeys/*.md, specs/testcases/*.md
# 输出: specs/design/pages.yaml, specs/ux/
```

### 参考文档

ai-ui-generator 包含以下参考资源：

| 文件 | 内容 |
|------|------|
| `references/component-catalog.md` | shadcn/ui 可用组件清单 |
| `references/layout-patterns.md` | 常见布局模式 |
| `references/interaction-patterns.md` | 交互模式库 |
| `references/neon-forge-tokens.md` | Neon Forge 设计 Token |

---

## 统一输出格式

无论使用哪个分支，最终都生成统一格式的设计规范：

### pages.yaml 格式

```yaml
pages:
  - id: page-id
    name: 页面名称
    route: /path
    description: 页面功能描述
    source: "Figma / Generated from {file}"

    sections:
      - id: section-id
        name: 区块名称
        layout: grid | flex | stack

        components:
          - id: component-id
            name: 组件名称
            type: card | list | form | button | ...
            shadcn_component: Card | Table | Form | ...
            data_source: post | category | user | ...
            fields:
              - field_name
            actions:
              - action_name
```

### specs/ux/ 结构

```
specs/ux/
├── README.md           # UX 规范概述
├── components/         # 可复用组件交互
├── pages/              # 每页交互规范
├── flows/              # 用户流程
├── forms/              # 表单规范
└── state/              # 状态管理
```

---

## AI 生成 Prompt（通用）

无论使用哪个分支，生成时都遵循以下原则：

```
依据原型是一个 hackathon 平台，用户可以发布活动、参与活动。
按照业界例子（如 Devpost、HackerEarth），给出完整的 UI 设计文档。

要求：
1. 布局架构
   - Header: 全局导航
   - Sidebar: 页面级导航（可折叠）
   - Body: 主内容区域
   - Panel: 详情/编辑面板（右侧滑出）
   - Footer: 版权信息

2. 用户旅程覆盖检查
   - 遍历 docs/user-journeys/ 每个流程
   - 确保每个流程都能在 UI 中找到入口
   - 标注流程对应的页面和组件

3. API Endpoint 检查
   - 为每个 UI 组件标注对应的 API endpoint
   - 检查 .synnovator/openapi.yaml 是否已实现
   - 若 endpoint 缺失，标注 "🚧 Not Implemented"

4. shadcn 组件映射
   - 优先使用 shadcn 现有组件
   - 自定义组件单独标注

5. 主题适配
   - 使用 Neon Forge 主题
   - Primary accent: #BBFD3B (Lime Green)
   - 深色背景 + 亮色强调色
```

---

## 验证

### User Journey 覆盖检查

| User Journey | UI 入口 | 相关页面 |
|-------------|---------|---------|
| J-001 用户注册 | Header → 注册按钮 | /register |
| J-002 创建活动 | Sidebar → 创建活动 | /events/new |
| J-003 提交作品 | 活动详情 → 提交按钮 | /events/[id]/submit |
| ... | ... | ... |

### Endpoint 覆盖检查

```
✅ GET /api/events — 已实现
✅ POST /api/events — 已实现
🚧 POST /api/events/{id}/submissions — Not Implemented
🚧 GET /api/notifications — Not Implemented
```

---

## 下一步

完成 UI 设计后，进入 [阶段 5-6: 前端配置](06-frontend-setup.md)。
